/**
 * v0.1 only ships SICP (web brief §"明确不做"). Centralized here instead of
 * scattered so swapping in `packs/index.json` + a real book picker later
 * (pitfall #10) is a one-file change, not a grep-and-replace.
 */
export const BOOK_ID = "sicp";
