/**
 * Fallback book when no active-book preference is stored yet (first run,
 * or the previously-active book was deleted). The Shelf tab is now a real
 * picker (see ActiveBookProvider.tsx) -- this is no longer the only book.
 */
export const DEFAULT_BOOK_ID = "sicp";
